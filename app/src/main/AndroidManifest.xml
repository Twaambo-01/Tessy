<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.my_cute_hotel">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.My_Cute_Hotel"
        tools:targetApi="31">

        <!-- Splash screen (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main page -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- Login -->
        <activity android:name=".LoginActivity" android:exported="true" />

        <!-- Register -->
        <activity android:name=".RegisterActivity" android:exported="true" />

        <!-- Room Selection -->
        <activity android:name=".SelectRoomActivity" android:exported="true" />

        <!-- Booking Details -->
        <activity android:name=".bookingDetailsActivity" android:exported="true" />

        <!-- Payment Method -->
        <activity android:name=".PaymentMethodActivity" android:exported="true" />


    </application>
</manifest>
